name: Push To Trunk
on:
  push:
    branches: [main, feat/*]
jobs:
  build-and-push:
    name: Build and Push Image
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout Repository
        uses: actions/checkout@v4
      
      - name: Build
        run: |
          echo "IMAGE_TAG=main-$(git rev-parse --short ${{ github.sha }})" >> $GITHUB_ENV
          echo "building image $IMAGE_TAG"

      - name: Push
        run: echo "pushing image ${{ env.IMAGE_TAG}}"
  deploy:
    name: Deploy to Environment
    environment: qa
    needs: build-and-push
    runs-on: ubuntu-latest
    steps: 
      - name: Deploy
        run: echo "deploying image ${{ env.IMAGE_TAG }} to ${{ env.TRIGGER_GROUP }}/${{ env.TRIGGER_PRODUCT}}/${{ env.TRIGGER_ENV }}/${{ env.TIGGER_LOCATION}}"
  create-release-tag: 
    name: Create Relase Tag
    needs: deploy
    runs-on: ubuntu-latest
    steps: 
      - name: Tag
        run: echo "create release tag prod only?"
  post-deploy-tests:
    name: Post Deploy Tests
    needs: deploy
    runs-on: ubuntu-latest
    steps:
      - name: Check deploy complete
        run: echo "wait til pods updated with new image"
      - name: Smoke test env
        run: echo "here we automate tests that new version up and running"
